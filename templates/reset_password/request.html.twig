<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Réinitialisez votre mot de passe{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
</head>
<body>
    {% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center align-items-center vh-100">
            <div class="col-md-6">
                {% for flash_error in app.flashes('reset_password_error') %}
                    <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
                {% endfor %}
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h1 class="h4 text-center">Réinitialisez votre mot de passe</h1>
                        <hr>
                        {{ form_start(requestForm) }}
                            <div class="mb-3">
                                {{ form_row(requestForm.email, {'attr': {'class': 'form-control'}}) }}
                            </div>
                            <div class="mb-3 text-muted text-center">
                                <small>
                                    Entrez votre adresse e-mail et nous vous enverrons un
                                    lien pour réinitialiser votre mot de passe.
                                </small>
                            </div>
                            <div class="d-grid">
                                <button class="btn btn-primary btn-block">Envoyer l'e-mail de réinitialisation</button>
                            </div>
                        {{ form_end(requestForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% endblock %}
</body>
</html>